# 月订单对比工具

一个纯前端页面工具，用于对比：
- 官方导出的月订单表
- 客户手动录入的月订单表

支持文件类型：WPS 另存的 `.xlsx` / `.xls` / `.csv`。

功能：
- 自动识别并选择订单号字段（可手动切换）
- 查找两边重复订单号
- 查找“官方有但客户漏掉”的订单号
- 查找“客户有但官方没有”的订单号
- 结果一键导出 CSV

直接打开 `index.html` 即可使用。
